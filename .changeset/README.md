# Changesets

Run `npm run changeset` to add a changeset when making changes to `linear-worktree`.

This generates a changeset file that describes the change and its semver bump type. Changesets are consumed during release to update the version and generate changelog entries.
