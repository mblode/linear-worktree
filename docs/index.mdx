---
title: Linear Worktree
description: Create a git worktree for a Linear issue and open it in Claude.
---

Create a git worktree for a Linear issue and open it in Claude.

## Installation

```bash
npm i -g linear-worktree
```

## Requirements

- Node.js >= 22
- `git`, `claude`, and [`cmux`](https://cmux.com/) on your PATH

See [Usage](/usage) for how to run it, set your Linear API key, and drive multiple worktrees at once.
